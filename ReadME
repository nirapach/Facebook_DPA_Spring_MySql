To Run the Project:
----------------------

Step 1: clone or download the zip file from the repository

Step 2: Go to the the directory
        Note: configure the config.properties file for respective properties

Step 3: Run the command
        "mvn clean install"

Step 4: Run the command
        "mvn clean package"

Step 5: Run the Command
        "mvn exec:java"

Step 6: Goto MYSQL Database and see the results